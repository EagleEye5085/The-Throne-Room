
<div class="center-horiz">
  <h1>The Throne Room</h1>
  <h6>It's the next big movement</h6>
</div>
<div class="navbar">
  <% if current_user %>
    <p><%= link_to "Create New Throne Room", "/thrones/new"%></p>
    <% end %>
  <% if !current_user %>
    <%= button_to 'Login', 'auth/google_oauth2', method: :get %>
  <% else %>
    <%= button_to 'Logout', '/logout', method: :delete %>
  <% end %>
</div>
 <!DOCTYPE html>
<html>
  <head>
    <title>Simple Map</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
      /* Always set the map height explicitly to define the size of the div
       * element that contains the map. */
      #map {
        height: 50%;
        width: 50%;
        margin-left: auto;
        margin-right: auto;
        display: block;
      }
      /* Optional: Makes the sample page fill the window. */
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body>
    <div id="map"></div>
    <script>
      var map;
      function initMap() {
        map = new google.maps.Map(document.getElementById('map'), {
          center: {lat: 39.7392, lng: -104.9903},
          // 39.7392° N, 104.9903° W
          zoom: 8
        });
      }
    </script>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%=ENV["map_api_key"]%>&callback=initMap"
    async defer></script>
  </body>
</html>
  <div class="row justify-content-md-center">
    <div class="pad-25-top pad-25-bottom">
      <div class="img">
        <%= image_tag("crown-clip-art-18.png") %>
      </div>
    </div>
  </div>

<h2> List of Thrones </h2>
<% @thrones.each do |throne| %>
  <h4> Name of Location: <%= throne.name %> </h4>
  <h4> Address: <%= throne.address %> </h4>
  <h4> Directions: <%= throne.directions %> </h4>
  <h4> <%= link_to "More Information", "/thrones/#{throne.id}" %> </h4>
  <br>
<% end %>




